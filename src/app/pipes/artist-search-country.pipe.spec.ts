import { ArtistSearchCountryPipe } from './artist-search-country.pipe';

describe('ArtistSearchCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistSearchCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
