import { ArtistSearchNamePipe } from './artist-search-name.pipe';

describe('ArtistSearchNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistSearchNamePipe();
    expect(pipe).toBeTruthy();
  });
});
