import { PaintingSearchAuthorPipe } from './painting-search-author.pipe';

describe('PaintingSearchAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new PaintingSearchAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
