import { SearchNamePipe } from './search-name.pipe';

describe('SearchNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNamePipe();
    expect(pipe).toBeTruthy();
  });
});
