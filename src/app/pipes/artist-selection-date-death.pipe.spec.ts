import { ArtistSelectionDateDeathPipe } from './artist-selection-date-death.pipe';

describe('ArtistSelectionDateDeathPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistSelectionDateDeathPipe();
    expect(pipe).toBeTruthy();
  });
});
