import { ArtistSelectionPipe } from './artist-selection.pipe';

describe('ArtistSelectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistSelectionPipe();
    expect(pipe).toBeTruthy();
  });
});
