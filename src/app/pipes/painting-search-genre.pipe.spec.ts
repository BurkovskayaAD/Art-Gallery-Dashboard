import { PaintingSearchGenrePipe } from './painting-search-genre.pipe';

describe('PaintingSearchGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new PaintingSearchGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
